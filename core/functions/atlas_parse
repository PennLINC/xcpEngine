atlas_parse() {
   local key keys
   a=()
   a[name]=${1}
   keys=$(echo "${atlas[${cxt}]}"|$JQ_PATH --raw-output '.'${a[name]}'|keys[]')
   for key in $keys
      do
      a[${key}]=$(eval echo $(json_query ${a[name]} ${key} "${atlas[${cxt}]}"))
   done
}
