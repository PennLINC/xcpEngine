atlas_parse() {
   local key keys
   a=()
   a[Name]=${1}
   keys=$(echo "${atlas[${cxt}]}"|$JQ_PATH --raw-output '.'${a[Name]}'|keys[]')
   for key in $keys
      do
      a[${key}]=$(eval echo $(echo "${atlas[${cxt}]}"|$JQ_PATH --raw-output '.'"${a[Name]}"'.'"${key}"))
   done
}
