derivative_typeof() {
   d_path=$(echo "${1}"|cut -d'#' -f3)
   nvol_derivative=$(fslnvols ${d_path})
   nvol_main=$(fslnvols ${2})
   if (( ${nvol_main} == ${nvol_derivative} ))
      then
      echo "timeseries"
   else
      echo "map"
   fi
}
