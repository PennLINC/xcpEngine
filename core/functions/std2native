std2native() {
   local i=${1}
   local o=${2}
   local n=${3}
   exec_sys rm -f ${o}
   exec_ants \
      antsApplyTransforms \
      -e 3 -d 3 \
      -i ${i} \
      -o ${o} \
      -r ${referenceVolume[${subjidx}]} \
      ${icoreg} \
      ${irigid} \
      ${iaffine} \
      ${iwarp} \
      ${iresample} \
      -n ${n}
}
