derivative_parse() {
   local key keys
   d=()
   d[name]=${1}
   keys=$(echo "${deriv[${cxt}]}"|$JQ_PATH --raw-output '.'${d[name]}'|keys[]')
   for key in $keys
      do
      d[${key}]=$(eval echo $(json_query ${d[name]} ${key} "${deriv[${cxt}]}"))
   done
}
