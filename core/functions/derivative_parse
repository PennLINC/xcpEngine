derivative_parse() {
   local key keys
   d=()
   d[Name]=${1}
   keys=$(echo "${deriv[${cxt}]}"|$JQ_PATH --raw-output '.'${d[Name]}'|keys[]')
   for key in $keys
      do
      d[${key}]=$(eval echo $(echo "${deriv[${cxt}]}"|$JQ_PATH --raw-output '.'"${d[Name]}"'.'"${key}"))
   done
}
